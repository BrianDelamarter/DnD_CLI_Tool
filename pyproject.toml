[project]
name = "dnd-cli-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "BrianDelamarter", email = "brian.d.delamarter@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
dnd-cli-tool = "dnd_cli_tool:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
